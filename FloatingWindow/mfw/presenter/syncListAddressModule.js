"use strict";

class SyncListAddressModule extends ModuleFloatingWindow {
    // constructor(option) {
    //     super(option);
    // }

    // _update(d) {
    //     this._view.update(this._getReturnData(d).data);
    // }

    // _backData() {
    //     const data = this._model.GetPreviousElement();
    //     this._view.execute(this._getReturnData(data));
    // }
    // _forwardData() {
    //     const data = this._model.GetNextElement();
    //     this._view.execute(this._getReturnData(data));
    // }
    // _getReturnData(data) {
    //     if (!data) return;
    //     const item = data.CurrentElement.split(";")[0];
    //     const system = data.CurrentElement.split(";")[1];
    //     const nextItem = data.NextElement.split(";")[0];
    //     const prevItem = data.PreviousElement.split(";")[0];

    //     data.NextElement = nextItem;
    //     data.PreviousElement = prevItem;

    //     return {
    //         item: item,
    //         system: system,
    //         data: data
    //     };
    // }
}